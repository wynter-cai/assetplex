# AgentHub 资产分类法（Asset Taxonomy）

> 版本: 1.0 | 日期: 2026-08-15 | 状态: 已定稿
>
> 本文档定义 AgentHub 产品的资产边界、分类标准和扩展路线。是产品的战略级文档，所有功能设计和技术实现都应以此为准。

---

## 一、设计目标

AgentHub 的核心价值是**让用户掌控自己的AI资产，换工具不丢东西**。

要达成这个目标，必须回答两个问题：
1. **AI工具有哪些核心资产？** — 决定 AgentHub 管理什么
2. **这些资产在切换工具时的迁移成本有多高？** — 决定 AgentHub 优先管理什么

本文档回答第一个问题，并用"迁移成本"作为分类标准。

---

## 二、分类标准：按迁移成本分级

我们用"**切换工具时丢失这个资产的痛感**"作为分类标准，因为：

- 用户关心的不是"工具里有什么文件"，而是"我换工具时哪些东西能跟着走"
- 越痛的资产，越值得 AgentHub 管理
- 不可迁移的资产，强行管理反而增加复杂度

按这个标准，资产分为 4 个梯队：

| 梯队 | 迁移成本 | 痛感 | 管理策略 |
|------|---------|------|---------|
| 🎯 第一梯队 | 低（语义级迁移即可） | 高（不带走就用不了工具） | ✅ 必须管理 |
| ⚠️ 第二梯队 | 中（需要格式转换） | 中高（丢了要重写工作流） | 🟡 建议管理 |
| 🔴 第三梯队 | 高（格式碎片化） | 极高（丢了多年积累的知识） | 🔴 强烈建议管理 |
| ⚫ 第四梯队 | 不可迁移 | 低（重新配置即可） | ❌ 不管理 |

---

## 三、资产分类总表

### 🎯 第一梯队：核心可迁移资产（当前已管理）

切换工具时必须带走，否则"换工具=从零开始"。

| 资产类别 | Code | 说明 | 跨工具兼容性 | 当前状态 |
|---------|------|------|-------------|---------|
| **身份** | `identity` | AI 角色认知、人设、行为准则 | ⭐⭐⭐⭐ 高（语义级迁移） | ✅ 已管理 |
| **技能** | `skill` | 领域知识、代码风格、专业能力描述 | ⭐⭐⭐⭐⭐ 极高（Markdown 通用） | ✅ 已管理 |
| **规则** | `rule` | 编码规范、安全约束、输出要求 | ⭐⭐⭐⭐⭐ 极高（规则可转译） | ✅ 已管理 |
| **MCP服务器** | `mcp` | 外部工具集成（数据库、API、文件系统） | ⭐⭐⭐⭐ 高（MCP 是标准协议） | ✅ 已管理 |

### ⚠️ 第二梯队：扩展资产（部分管理，待补全）

切换工具后恢复原工作流的关键，丢失后要重新构建。

| 资产类别 | Code | 说明 | 跨工具兼容性 | 当前状态 |
|---------|------|------|-------------|---------|
| **斜杠命令** | `command` | 自定义命令（如 `/generate-test`、`/commit`） | ⭐⭐⭐⭐ 高（本质是"提示词+参数"） | 🟡 仅扫了 Claude Code |
| **子代理** | `agent` | 子代理配置（专门处理某类任务的子 AI） | ⭐⭐⭐ 中（模型选择可能不同） | 🟡 仅扫了 Claude Code |
| **提示词模板** | `prompt` | 复杂提示词模板库（重复使用的） | ⭐⭐⭐⭐ 高（文本即可） | 🔴 未管理 |
| **智能体定义** | `agent-config` | 完整智能体定义（模型/温度/工具集） | ⭐⭐ 低（工具专属字段多） | 🔴 未管理 |

### 🔴 第三梯队：隐性资产（未管理，强烈建议补全）

用户花最多时间积累，但最容易丢的资产。

| 资产类别 | Code | 说明 | 跨工具兼容性 | 当前状态 |
|---------|------|------|-------------|---------|
| **长期记忆** | `memory` | 用户偏好、项目决策、踩过的坑 | ⭐⭐⭐⭐ 高（文本即可） | 🔴 TRAE 扫了一部分 |
| **知识库** | `knowledge` | 项目知识库、文档索引、RAG 数据 | ⭐⭐ 低（格式差异大） | 🔴 未管理 |
| **项目上下文** | `project-context` | 项目级规则、技术栈约束 | ⭐⭐⭐⭐ 高（文本即可） | 🔴 未管理 |
| **工作流** | `workflow` | 自定义工作流（多步骤AI任务） | ⭐⭐ 低（概念模型差异大） | 🔴 未管理 |

### ⚫ 第四梯队：工具专有资产（不管理）

无法跨工具迁移，强行管理反而增加复杂度。

| 资产 | 为什么不管理 |
|------|-------------|
| UI 偏好（主题、布局、字体） | 工具专属，不构成"能力" |
| 快捷键绑定 | 工具专属键位映射 |
| 账号/认证（OAuth Token、API Key） | 隐私数据且会过期 |
| 本地缓存（模型缓存、向量索引） | 重新生成即可 |
| 会话窗口状态（当前打开的标签页） | 瞬时状态，非资产 |

---

## 四、各AI工具资产盘点

### 4.1 TRAE 中国版

**配置目录**: `~/.trae-cn/`

| 资产 | 路径 | 格式 | AgentHub 状态 |
|------|------|------|---------------|
| 身份 | `memory/user_profile.md` | Markdown | ✅ 已扫 |
| 长期记忆 | `memory/*.md` (其他) | Markdown | 🟡 部分扫 |
| 技能 | `skills/*.md` | Markdown | ✅ 已扫 |
| 规则 | `rules/*.md` | Markdown | ✅ 已扫 |
| MCP | `mcp.json` | JSON | ✅ 已扫 |
| 项目规则 | `<project>/.trae/rules/` | Markdown | ❌ 未扫（项目级） |

### 4.2 Claude Code

**配置目录**: `~/.claude/`

| 资产 | 路径 | 格式 | AgentHub 状态 |
|------|------|------|---------------|
| 身份 | `CLAUDE.md` | Markdown | ✅ 已扫 |
| 技能 | `skills/*.md` | Markdown | ✅ 已扫 |
| 规则 | `rules/*.md` | Markdown | ✅ 已扫 |
| 斜杠命令 | `commands/*.md` | Markdown | ✅ 已扫 |
| 子代理 | `agents/*.md` | Markdown | ✅ 已扫 |
| MCP | `.claude.json` | JSON | ✅ 已扫 |
| 长期记忆 | `memory/*.md` | Markdown | ❌ 未扫 |
| 项目规则 | `<project>/.claude/CLAUDE.md` | Markdown | ❌ 未扫（项目级） |

### 4.3 Codex

**配置目录**: `~/.codex/`

| 资产 | 路径 | 格式 | AgentHub 状态 |
|------|------|------|---------------|
| 身份 | `AGENTS.md` | Markdown | ✅ 已扫 |
| 技能 | `skills/*.md` | Markdown | ✅ 已扫 |
| MCP | `config.toml` | TOML | ✅ 已扫 |
| 子代理 | `config.toml` (内嵌) | TOML | ❌ 未扫 |
| 项目规则 | `<project>/AGENTS.md` | Markdown | ❌ 未扫（项目级） |

### 4.4 Qoder

**配置目录**: 项目级 `.qoder/`（无统一用户级目录）

| 资产 | 路径 | 格式 | AgentHub 状态 |
|------|------|------|---------------|
| 技能 | `.qoder/skills/` | Markdown | ✅ 已扫 |
| 规则 | `.qoder/rules/` | Markdown | ✅ 已扫 |
| Quest | `.qoder/quests/` | Markdown | ✅ 已扫 |
| 知识库 | `.qoder/knowledge/` | Markdown | ✅ 已扫 |
| 身份 | `AGENTS.md`（项目级） | Markdown | ✅ 已扫 |

**注意**: Qoder 是项目级工具，没有"用户级身份"概念，身份是每个项目独立的。

### 4.5 WorkBuddy

**配置目录**: `~/.workbuddy/`

| 资产 | 路径 | 格式 | AgentHub 状态 |
|------|------|------|---------------|
| 技能 | `skills/*.md` | Markdown | ✅ 已扫 |
| 规则 | `rules/*.md` | Markdown | ✅ 已扫 |
| MCP | `.mcp.json` | JSON | ✅ 已扫 |
| 身份 | （未发现明确身份文件） | - | ❌ 工具本身可能不支持 |

---

## 五、扩展路线图

### Phase A：核心4类（当前已完成）

管理：`identity`、`skill`、`rule`、`mcp`

**目标**: 解决"换工具能不能继续干活"的问题。

**状态**: ✅ 已实现，正在做 UI 重构（v2 设计方案）

### Phase B：扩展3类（UI 重构完成后）

新增：`command`、`agent`、`memory`

**目标**: 解决"换工具能不能恢复原工作流"的问题。

**具体改动**:

| 改动点 | 详情 |
|--------|------|
| 类型定义 | `AssetCategory` 加 `'command' \| 'agent' \| 'memory'` |
| Adapter | 每个工具补全 scan 范围（Claude Code 已有 command/agent，其他工具按实际情况补） |
| Scanner | 扫描 `commands/`、`agents/`、`memory/` 目录 |
| 前端 | 资产库左侧导航支持动态类别，不是写死 4 个 |
| 数据迁移 | 无需迁移旧数据（向后兼容） |

**新增资产类别详细定义**:

#### `command`（斜杠命令）
- **是什么**: 用户自定义的高频操作，如 `/generate-test`、`/commit`
- **格式**: Markdown 文件，含命令名、参数定义、提示词模板
- **扫描位置**:
  - Claude Code: `~/.claude/commands/*.md`（已扫）
  - Codex: `~/.codex/commands/`（待确认）
  - TRAE: 暂无对应概念
- **跨工具兼容性**: 高，本质是"参数化提示词"

#### `agent`（子代理配置）
- **是什么**: 专门处理某类任务的子 AI 配置（如"代码审查专家"）
- **格式**: Markdown 文件 + 可选 JSON 配置
- **扫描位置**:
  - Claude Code: `~/.claude/agents/*.md`（已扫）
  - Codex: `~/.codex/config.toml` 中的 agents 字段（未扫）
  - TRAE: 暂无对应概念
- **跨工具兼容性**: 中（模型选择/工具集可能不同，但角色定义可迁移）

#### `memory`（长期记忆）
- **是什么**: 用户偏好、项目决策、踩过的坑
- **格式**: Markdown 文件
- **扫描位置**:
  - TRAE: `~/.trae-cn/memory/*.md`（部分已扫）
  - Claude Code: `~/.claude/memory/*.md`（未扫）
- **跨工具兼容性**: 高（纯文本）

### Phase C：隐性资产（远期）

新增：`knowledge`、`project-context`、`workflow`

**目标**: 解决"换工具能不能带走多年积累的隐性知识"的问题。

**难点**:
- `knowledge` 各工具格式差异大（Qoder 用 `.qoder/knowledge/`，其他工具可能用向量库）
- `project-context` 需要引入"项目"概念，AgentHub 需要从"工具级管理"扩展到"项目级管理"
- `workflow` 概念模型差异大，需要抽象出统一的工作流模型

**建议**: 等 Phase A、B 跑通后再启动，且需要单独的 spec 来定义"项目"这个新概念。

---

## 六、设计原则

基于本分类法，AgentHub 在设计时应遵循以下原则：

### 1. 资产类别可扩展
- 前端导航、后端类型、数据库 schema 都不能写死 4 个类别
- 新增类别应该是"加配置"而不是"改代码"

### 2. 工具能力差异要尊重
- 不是所有工具都有所有资产类别（如 Qoder 没有用户级 identity）
- UI 上要显示"该工具支持哪些类别"，而不是强行让所有工具对齐

### 3. 资产类别的优先级
- 第一梯队是 P0，必须有
- 第二梯队是 P1，UI 重构后立即补
- 第三梯队是 P2，远期规划
- 第四梯队永远不做

### 4. 跨工具兼容性是核心指标
- 资产管理不仅是"存进来"，还要"分发出去"
- 分发时如果目标工具不支持某类资产，要明确告知用户
- 例如：把 Claude Code 的 `commands/` 同步到 TRAE 时，TRAE 不支持斜杠命令，应该提示用户

---

## 七、与现有文档的关系

| 文档 | 关系 |
|------|------|
| [assetplex-constitution.md](file:///d:/Trae/Agent武器库/设计文档/assetplex-constitution.md) | 宪法级原则，本文档是其下位文档，定义"管什么" |
| [assetplex-ui-redesign-v2.md](file:///d:/Trae/Agent武器库/设计文档/assetplex-ui-redesign-v2.md) | UI 设计方案，资产库导航要按本文档的分类设计 |
| [AGENTS.md](file:///d:/Trae/Agent武器库/AGENTS.md) | 项目级开发规则，本文档是其引用的资产分类标准 |

---

## 八、版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-08-15 | 初版，定义 4 梯队分类法和扩展路线图 |
